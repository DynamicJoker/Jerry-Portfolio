# Beta Work in Progress Repository for Jerry James

## Content updates

Edit portfolio content in `src/content.js`, then run `npm run generate:static` to refresh the bot-readable HTML, JSON-LD, `llms.txt`, `robots.txt`, and sitemap files. Use `npm run check:static` before committing to confirm the generated files are in sync.
