{
  "name": "jerry-james-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:static": "node scripts/render-static-content.mjs",
    "check:static": "node scripts/render-static-content.mjs --check",
    "prebuild": "npm run generate:static",
    "build": "vite build --configLoader runner",
    "preview": "vite preview --configLoader runner"
  },
  "dependencies": {
    "@fontsource/geist": "^5.0.0",
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "browserslist": "^4.24.0",
    "lightningcss": "^1.29.0",
    "vite": "^6.0.0"
  }
}
